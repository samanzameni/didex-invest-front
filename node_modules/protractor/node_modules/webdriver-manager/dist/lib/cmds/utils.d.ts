import * as yargs from 'yargs';
import { Options } from './options';
import { OptionsBinary } from './options_binary';
/**
 * Converts an options object into an options binary object.
 * @param options
 */
export declare function addOptionsBinary(options: Options): OptionsBinary;
/**
 * Create the options with all providers. Used for clean and status commands.
 * @param argv
 */
export declare function convertArgs2AllOptions(argv: yargs.Arguments): Options;
/**
 * Create the options with providers depending on argv's. Used for update and
 * start commands.
 * @param argv
 */
export declare function convertArgs2Options(argv: yargs.Arguments): Options;
